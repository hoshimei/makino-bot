{
    "name": "makino-bot",
    "version": "1.0.0",
    "description": "Telegram bot for updating group titles and avatars",
    "main": "dist/index.js",
    "scripts": {
        "build": "esbuild src/index.ts --bundle --outfile=dist/index.js --format=esm --target=es2022",
        "dev": "wrangler dev",
        "deploy": "wrangler deploy"
    },
    "dependencies": {
        "dayjs": "^1.11.10"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20231025.0",
        "esbuild": "^0.19.0",
        "typescript": "^5.2.0",
        "wrangler": "^3.0.0"
    }
}